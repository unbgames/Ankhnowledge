sudo: required

language: c++

dist: trusty

compiler:
  - gcc

install: ./install-ankh.sh

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master